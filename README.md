# LetMeKnow

The deployed text root (`/`) contains API usage.

## Local development and tests

```bash
npm install
npm run dev
npm test
```

## Deployment

Cloudflare's dashboard Git integration deploys the configured production branch. The branch and integration settings are managed in the dashboard, not in this repository.
